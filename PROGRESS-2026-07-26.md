# content-pipeline-stack 进度 (2026-07-26 02:40)

## 总览
- **架构**: 9 服务 docker stack (4 default + 5 v6-workers profile)
- **registry**: 本地 registry:2 (port 5000, 567MB / 10 image layer 去重)
- **纯源码模式**: 所有路径 ENV-driven (.env + .env.example)

## ✅ Day 5 (2026-07-26 02:00-02:40) — 全部 healthy

### 9 服务最终状态

| 服务 | 健康 | 备注 |
|---|---|---|
| content-pipeline-mcp | ✅ healthy | port 28092→18092 |
| file-service | ✅ healthy | port 28098→18098 |
| v6-monitor | n/a | SIGUSR1 daemon, 无 healthcheck |
| v6-enqueue | ✅ healthy | sleep infinity + cron |
| v6-download | ✅ healthy | chromium bind mount |
| v6-transcribe | ✅ healthy | 调 hk-asr (extra_hosts) |
| v6-distill | ✅ healthy | 调 KB_DATA_DIR |
| v6-upload | ✅ healthy | 调 file-service:18098 (容器网络) |

### douyin-recorder 状态
- systemd douyin-recorder active (PID 1136470, 我手动 sudo start)
- docker douyin-recorder stopped (避免双写)
- 等 Cove 拍板 (d) 完全切 docker

### 13 commits
```
3213bb3 fix(stack): 纯源码模式 - ENV-driven paths + 7 healthcheck bug 修复
d698920 fix(stack): 5 worker CMD -m → 直接调脚本 + 起 registry:2 改本地推
1842ab5 feat(stack): 5 v6 worker Dockerfile + docker-compose v6-workers profile
2d39503 fix(stack): 移除重复 douyin-recorder block + 切/回滚脚本
e35df9a docs(stack): Day 4 持久化设计
e7ddd29 docs(stack): Day 3 (a)+(b) 完成
b762724 feat(douyin-recorder): 容器化
63cf0a2 fix(file-service): healthcheck wget → curl
d47a35c feat(v6-monitor): 容器化 + SIGUSR1 验证通过
3abae09 docs(stack): Day 2 完成
d382a8d fix(file-service): UID 1001:1001 + 补 bind uploads + .openclaw volumes
4bf658f fix(file-service): alpine → bookworm-slim sharp prebuilt
bd71ec4 docs(stack): README + Dockerfile shell-syntax 修复
72d5358 feat(stack): docker-compose 初始化
```

### 本次修的 7 个 healthcheck bug

1. **v6-download/queue/enqueue**: `/data/queue.db` (写死) → `${V6_WORKDIR}/queue.db` (ENV)
2. **v6-transcribe**: `host.docker.internal:18200` 写死 → `${HK_ASR_URL}` + extra_hosts
3. **v6-upload**: `host.docker.internal:18098` (实际不通, host 18098=systemd file-service) → `file-service:18098` (容器网络)
4. **v6-distill**: `/data/knowledge-base` → `${KB_DATA_DIR}`
5. **file-service healthcheck**: `curl not found` → COPY healthcheck.js + node http 模块
6. **file-service multi-stage**: COPY healthcheck.js 在 stage 1, stage 2 拿不到 → 加 `COPY --from=builder`
7. **v6-upload healthcheck CMD**: 还用写死 host.docker.internal → `${FILE_SERVICE_URL}`

### 永久教训 (本轮 5 条)

1. **纯源码模式 = ENV-driven paths**, 任何硬编码主机路径 = 反开源模式
2. **host.docker.internal vs container network**: 
   - 调同 compose 内 service → 用 container name (file-service:18098) 
   - 调 host 上 systemd 服务 → 用 host.docker.internal (172.18.0.1)
   - 别混, 不然 systemd 占端口会让 docker 不通
3. **multi-stage build COPY 不会跨 stage 自动同步**, 必须 `COPY --from=builder`
4. **systemd vs docker 端口冲突**: systemd 用 18098, docker 用 28098 (错位), 同端口=冲突
5. **Cove 拍板: "纯源码 + 自己数据"** = 开源模式, 别人 clone + 自己配数据, 不需要数据迁移脚本

### 后续

- (d) 完全切 systemd→docker recorder (等 Cove sudo)
- (e)(f)(g) 多机房同步 + 灾备 + 容器写权限
- init.sh + README (让别人 clone 仓库真能跑, 纯源码模式)

